#include <iostream>
using namespace std;

int main() {
    int n, m, o, avg;
    cout << "Enter three numbers: ";
    cin >> n >> m >> o;
    avg = (n + m + o) / 3;
    cout << "Average: " << avg << endl;
}